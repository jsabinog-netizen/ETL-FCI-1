version: 2

models:

  - name: stg_empresa_giz
    description: "Catálogo de empresas aliadas GIZ."
    columns:
      - name: id
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn
      - name: nit
        tests:
          - not_null:
              config:
                severity: warn