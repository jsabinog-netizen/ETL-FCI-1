version: 2

models:

  - name: stg_vacantes_giz
    description: "Catálogo de vacantes disponibles GIZ."
    columns:
      - name: id
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn
      - name: vacante
        tests:
          - not_null:
              config:
                severity: warn