version: 2
models:
- name: stg_ge_agendamiento
  description: 'Ruta Mujer: GE_Agendamiento. Grano: un registro Zoho identificado
    por id.'
  columns:
  - name: id
    tests:
    - unique:
        config:
          severity: warn
    - not_null:
        config:
          severity: warn
  - name: modified_time
    description: Modified_Time de Zoho convertido de forma segura a TIMESTAMP.
