version: 2
models:
- name: stg_pre_registro_empresarial
  description: 'Ruta Mujer: Pre_registro_Empresarial. Grano: un registro Zoho identificado
    por id.'
  columns:
  - name: id
    tests:
    - unique:
        config:
          severity: warn
    - not_null:
        config:
          severity: warn
  - name: modified_time
    description: Modified_Time de Zoho convertido de forma segura a TIMESTAMP.
