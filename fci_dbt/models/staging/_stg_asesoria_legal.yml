version: 2

models:
  - name: stg_asesoria_legal
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: estado_asesoria_legal
        tests:
          - accepted_values:
              arguments:
                values: ['programado', 'cancelado', 'ejecutado']