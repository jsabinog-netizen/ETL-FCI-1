version: 2

models:
  - name: stg_registro_empresas
    columns:
      - name: id
        tests:
          - unique
          - not_null

      - name: estado_diagnostico
        tests:
          - accepted_values:
              arguments:
                values: ['programado', 'cancelado', 'ejecutado']