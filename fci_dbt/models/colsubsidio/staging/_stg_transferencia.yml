version: 2

models:
  - name: stg_transferencia
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: estado_transferencia
        tests:
          - accepted_values:
              arguments:
                values: ['programado', 'cancelado', 'ejecutado']